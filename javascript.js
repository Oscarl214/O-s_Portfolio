// HOPING TO ADD JS TO BE ABLE TO HAVE BUTTON ONLY APPEAR WHEN USER SCROLLS PAST HEADER SECTIO

function doStuff(callback) {
  callback();
}

doStuff(function () {
  document.body.className = "visible";
});
